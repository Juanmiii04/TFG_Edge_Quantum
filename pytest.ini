[pytest]
; Añadimos la raíz del proyecto al pythonpath para poder hacer "from src.xxx import yyy"
; igual que se hace en main_experimentos.ipynb, sin necesidad de instalar el proyecto como paquete.
pythonpath = .
markers =
    slow: tests lentos que ejecutan QAOA de verdad (simulación cuántica completa)
